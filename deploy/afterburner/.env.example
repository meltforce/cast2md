# RunPod Afterburner Configuration
# Copy to .env and fill in your values
# Usage: source deploy/afterburner/.env

# Required: RunPod API key (Settings -> API Keys)
export RUNPOD_API_KEY=

# Required: Tailscale auth key (ephemeral, reusable, with tag:runpod)
# Generate at: https://login.tailscale.com/admin/settings/keys
export TS_AUTH_KEY=

# Optional: Override defaults
# export CAST2MD_SERVER_URL=https://cast2md.leo-royal.ts.net
# export TS_HOSTNAME=runpod-afterburner
# export RUNPOD_GPU_TYPE=NVIDIA GeForce RTX 4090
# export AFTERBURNER_MODE=github
