# RunPod Afterburner Configuration
# Copy to .env and fill in values, then: source .env

# Required: RunPod API key (from https://www.runpod.io/console/user/settings)
export RUNPOD_API_KEY=

# Required: Your cast2md server URL (Tailscale hostname)
export CAST2MD_SERVER_URL=https://your-server.your-tailnet.ts.net

# Optional: Override defaults
# export TS_HOSTNAME=runpod-afterburner
# export RUNPOD_GPU_TYPE="NVIDIA GeForce RTX 4090"
# export GITHUB_REPO=meltforce/cast2md

# Note: The Tailscale auth key is stored in RunPod Secrets (not here)
# Create a secret named "ts_auth_key" at https://www.runpod.io/console/user/secrets
