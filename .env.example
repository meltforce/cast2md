# cast2md Configuration
# Copy this file to .env and customize as needed

# PostgreSQL database connection (required)
# For local development with docker-compose:
DATABASE_URL=postgresql://cast2md:dev@localhost:5432/cast2md

# Storage path for audio files and transcripts
STORAGE_PATH=./data/podcasts

# Temporary download path (should be on same filesystem as STORAGE_PATH for atomic moves)
TEMP_DOWNLOAD_PATH=./data/temp

# Whisper model configuration
# Options: tiny, base, small, medium, large-v2, large-v3
WHISPER_MODEL=base

# Whisper device: cpu, cuda, auto
WHISPER_DEVICE=auto

# Compute type: int8, float16, float32 (depends on device)
WHISPER_COMPUTE_TYPE=int8

# Download settings
MAX_CONCURRENT_DOWNLOADS=2
MAX_RETRY_ATTEMPTS=3

# Request timeout in seconds
REQUEST_TIMEOUT=30
